/// Enum to represent the type of item being shared.
enum ShareableItemType {
  experience,
  category,
}

/// Enum to represent the access level granted in a share.
enum ShareAccessLevel {
  view,
  edit,
}
