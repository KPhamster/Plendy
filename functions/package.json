{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "overrides": {
    "node-forge": "^1.3.2",
    "jws": "^4.0.1",
    "jsonwebtoken": {
      "jws": "^3.2.3"
    }
  },
  "scripts": {
    "lint": "eslint . --fix",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "bulk:shareperms:dryrun": "echo Use deployed HTTPS endpoint /bulkDeleteSharePermissions with dryRun=true",
    "bulk:shareperms:delete": "echo Use deployed HTTPS endpoint /bulkDeleteSharePermissions with confirm=yes"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "cors": "^2.8.5",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^3.1.0"
  },
  "private": true
}
